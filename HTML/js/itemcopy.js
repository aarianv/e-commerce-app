import {products} from "../datasets/products";

export function ref(){
    search = products.image;

    const found = array.find(obj => obj.image === search.image);
    
    let result;
    if(found) {
    result = found;
    };
}
    